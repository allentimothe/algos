// - push - O(1)
// - pop - O(1)
// - shift - O(N)
// - unshift - O(N)
// - concat - O(N) // Concat merges 2 arrays together
// - slice - O(N) // returns a copy of an array or particular parts of the array
// - splice - O(n) // removes and adds new elements
// - sort - O(N*log N)
// - forEach/map/filter/reduce/etc. - O(N)